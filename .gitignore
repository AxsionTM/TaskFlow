# Dependencies
node_modules/
**/node_modules/

# Next.js
.next/
out/

# Build output
dist/

# Environment / secrets
.env
.env.*
!.env.example

# TypeScript
*.tsbuildinfo

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local data / uploads
*.sqlite
*.sqlite3

# Android phone builds (release APKs are built locally, not stored in git)
*.apk
*.aab

# Android release signing secrets (local only — never commit)
*.keystore
*.jks
keystore.properties

# Local Android SDK path + local Gradle JDK path (machine-specific)
apps/web/android/local.properties
apps/web/android/gradle.properties

# Capacitor generated web copy for the native shell (regenerated by `npx cap sync`)
apps/web/capacitor-www/

# Local helper scripts / markers for phone builds
build-apk.bat
apps/web/android/failed.txt
